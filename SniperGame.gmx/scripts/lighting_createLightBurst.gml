///lighting_createLightBurst( x, y, color, alpha, depth, falloff, duration );

with ( instance_create( argument0, argument1, lightBurst ) ) {
    image_blend = argument2;
    image_alpha = argument3;
    depth = argument4;
    falloff = argument5;
    duration = argument6
    
}

